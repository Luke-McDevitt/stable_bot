# stewart_vision/docs

The full specification for the closed-loop ball demos lives in
`../../stewart_bringup/docs/closed_loop_ball_demos.md` (and `.txt` /
`.docx` siblings) — that file is the source of truth.

This directory holds vision-package-specific notes that don't belong
in the cross-cutting spec.

(empty for now)
